version: "3"

services:

  postgres:
    image: postgres
    environment:
      POSTGRES_DB: openmes
      POSTGRES_USER: openmes
      POSTGRES_PASSWORD: openmes
    ports:
      - "5432:5432"

  api:
    build: .
    ports:
      - "5000:80"